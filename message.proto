syntax = "proto3";

package message;

message Header{
  uint32 type32  = 1;
  uint32 service = 2;
  uint32 flags   = 3;
  bytes  nodeID  = 4;
  bytes  body    = 6;
  uint32 id      = 7;
}

// will probably need to replace nodeID with pub and shared, but this will
// work for now